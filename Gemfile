# frozen_string_literal: true

source "https://rubygems.org"

gem 'sinatra'

group :sinatra_plugins do
  gem 'sinatra'
end

gem 'shotgun'

group :shotgun_plugins do
  gem 'shotgun'
end

gem 'capybara'

gem 'selenium-webdriver'


git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
